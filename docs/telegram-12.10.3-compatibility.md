# Telegram 12.10.3 适配记录

2026-09-18，在 `192.168.6.13:5555` 验收；模块版本 `12.10.3` / versionCode `14`。

## 构建选择与兼容

- 目标包：`org.telegram.messenger`，Telegram `12.10.3` / `70892`。
- base APK SHA-256：`6b3565f20af6681172b49cf84915818fb575ace8295cd1de08c4b9a2b3985f5e`。
- 新增独立的 `telegram-70892.tsv`，包含 2,958 个类、4,269 个字段、1,062 个方法别名。
- APK 精确哈希选择对应映射，保留原 `70862` / `12.10.2` 映射；未知构建保持原名称查找路径。同版本号其他渠道包不自动视为已验收。
- 映射根据两版归档 APK 的指令、完整方法描述符、类结构和引用关系匹配；Story 横栏、RecyclerView 附着回调以目标 smali 复核。生成记录保留于本地 `dist/telegram-12.10.3-device13-20260918/`。
- 新映射所有条目均能在目标 APK 找到对应声明；不将这种静态检查等同于全部功能行为通过。

## 实机结果

- 原生设置中 GramSieve 行可见，实际点击后 `configPanelOpen=true`。
- 用户原先开启的隐藏 Story 横栏、隐藏联系人页、隐藏首页悬浮按钮在冷启动后继续生效；界面探针与截图一致。
- 配置文件升级前后完全一致，包含原有私密媒体保存、即时相机等开关；本次没有改动用户设置。
- 功能探针与上一版保持一致：37 项 `registered_behavior_unverified`、2 项 `external_owner`、2 项 `static_flags_only`、15 项原有 `unavailable_in_build`；没有新增缺失或不匹配。
- 历史加载适配器报告 `supported`，启动与设置入口交互后的持久日志没有 ERROR、WARN 或 unsupported。
- 设备使用原有 Debug 签名覆盖安装，保留应用数据；公开 APK 使用正式签名。未发送、转发或上传测试内容。

## 验证范围

- 相关 JVM 测试与 Debug 构建通过；正式发布执行全量 JVM 测试、lintDebug、assembleRelease、版本元数据、签名、哈希和差异检查。
- 仍未在本次完整验收消息撤回/编辑、视频下载完成/取消、语音与 Story 保存、受限内容转发、输入状态和已读回执等端到端场景。不扩大“已测试功能”清单。
- 原有功能未删除或禁用；不需要新增配置。审核由主代理单人完成。
- APK 归档、设备原包及证据保留用于复核和回滚；无新建 worktree 或清理操作。
