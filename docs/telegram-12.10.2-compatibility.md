# Telegram 12.10.2 适配记录

2026-09-17，在 `192.168.6.13:5555` 验证。GramSieve 为 `12.10.2` / versionCode `13`，后续显示版本号跟随目标 Telegram，内部版本码独立递增。

## 目标构建

- 包名：`org.telegram.messenger`
- Telegram：`12.10.2` / versionCode `70862`
- base APK SHA-256：`b923544110654a2c0ae4e0d5829c1606b9f8807f76088fb4142622a93e408f8e`
- 设备：Android 16，LSPosed 2.2.0，API 102。

`TelegramSymbols` 仅在 base APK 哈希完全一致时加载 `telegram-70862.tsv`。其他构建继续使用原有名称查找；同版本号的其他渠道包不自动视为已适配。旧构建的原有方法签名保留。

映射基于本地归档的 70382 / 12.10.1 和 70862 / 12.10.2 APK，结合协议构造器 ID、类结构、指令引用及 Telegram 官方源码定位；关键变更以目标 smali 签名复核。映射文件中的 C/F/M 分别表示类、字段和方法；方法使用完整 JVM 描述符区分重载。归档和提取脚本留在本地，APK、反编译结果及设备私人数据不入库。

## 行为变化

- 反射路径解析混淆后的类、字段及方法，覆盖消息绑定、过滤、防撤回、下载和增强功能。
- 兼容下载按钮参数删减、滚动回调参数变化、设置列表及 Story 保存的静态回调。
- RecyclerView 绑定入口被内联后，从获取 ViewHolder 的保留入口处理绑定结果。
- 支持新版设置行工厂及动态副标题；保留旧签名回退。
- 雪花动画兼容内联后的状态字段，关闭功能或重新加载时恢复原状态。
- 启动时尚未取得 Application 的配置读取不再缓存默认值，避免已开启的联系人隐藏开关在冷启动时失效。

## 已验证与边界

- 原生 Telegram 设置中的 GramSieve 行可见，点击能够打开模块面板。
- 通过真实控件监听器修改开关，立即持久化，无需保存按钮。
- 联系人入口即时隐藏，重启后仍隐藏；下载入口常驻开关持久化且日志确认原生入口启用。
- 验收后两个测试开关恢复关闭；配置内容与验收前一致，仅更新时间变化。
- 最后一次启动及交互的持久日志未发现 ERROR、WARN 或 unsupported。
- 功能探针：37 项为已注册但未逐项行为验收，2 项由外部实现负责，2 项为静态标志，15 项保持原有 unavailable 分类；无新增类、方法或字段缺失。历史加载适配器报告 supported。
- 全量 JVM 测试 287 项通过；lint 为 0 errors / 177 warnings；后续签名和冷启动修正的相关测试及 Debug 构建通过；`git diff --check` 通过。
- 未新增删减或禁用功能。已注册不等于全部功能已经端到端通过：受限消息、语音、Story、发送/转发、已读/输入状态、防撤回与下载完成等场景没有在本次重新完整验收，不扩大“已测试”清单。

## 设备制品

设备原模块使用 Debug 签名，本次以相同证书覆盖安装，保留应用数据。此文件为设备验收包，不是正式签名的公开 Release。

- 文件：`dist/telegram-12.10.2-device13-20260917/GramSieve-v12.10.2-debug.apk`
- APK SHA-256：`987c005957705a5652944a44c3103a47d91bbdb914cfbf7591a872abaf2ea24d`
- 签名 SHA-256：`3ba8bacf8e74b4d89dbc860bc87df2cb25a2cf3cded4d0947cda5b3923052e9a`
- Gradle、output-metadata 和 aapt 均为 `13` / `12.10.2`。
- 原设备 APK 和版本归档保留用于回滚；本次没有公开发布、发送聊天消息或执行清理。
